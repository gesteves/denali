<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="chrome=1">
  <meta name="viewport" content="width=device-width">
  <title><%= @page_title.nil? ? '' : sanitize("#{@page_title} · ") %><%= @photoblog.name %></title>
  <%= stylesheet_link_tag 'admin', media: 'all' %>
  <%= csrf_meta_tags %>
  <%= javascript_pack_tag 'admin', defer: true %>
  <%= javascript_include_tag 'https://use.fontawesome.com/releases/v5.0.10/js/all.js', defer: true %>
</head>
<body>
  <%= render partial: 'partials/admin/nav' %>

<section class="section">
  <div class="container">
    <% flash.each do |name, message| -%>
    <div class="notification is-<%= name %>" data-controller="flash">
      <button class="delete" data-action="click->flash#close"></button>
      <%= message %>
    </div>
    <% end %>
    <%= yield %>
  </div>
</section>

</body>
</html>
