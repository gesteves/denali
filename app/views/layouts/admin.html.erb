<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="chrome=1">
  <meta name="viewport" content="width=device-width">
  <meta http-equiv="Accept-CH" content="DPR, Width, Viewport-Width">
  <title><%= @page_title.nil? ? '' : raw("#{@page_title} &middot; ") %><%= @photoblog.name %></title>
  <%= stylesheet_link_tag 'admin', media: 'all' %>
  <%= csrf_meta_tags %>
  <script src="https://use.typekit.net/zwv6nvu.js" onload="try{Typekit.load({async:true});}catch(e){}" async></script>
</head>
<body>

<% if content_for? :nav %>
  <%= yield :nav %>
<% else %>
  <%= render partial: 'partials/admin/nav', locals: { active_nav: 'none' } %>
<% end %>

<div class="content">
  <% flash.each do |name, message| -%>
    <div class="flash flash--<%= name %>">
      <p class="flash__message"><%= message %></p>
      <a href="#" class="flash__close js-flash-close">&times;</a>
    </div>
  <% end %>
  <%= yield %>
</div>

<%= javascript_include_tag 'admin' %>
<% unless ENV['dropbox_key'].nil? %>
  <script type="text/javascript" src="https://www.dropbox.com/static/api/2/dropins.js" id="dropboxjs" data-app-key="<%= ENV['dropbox_key'] %>"></script>
<% end %>
</body>
</html>
