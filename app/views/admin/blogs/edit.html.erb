<%= form_for(@blog, url: admin_settings_update_path, html: { class: 'form' }) do |f| %>
  <% if @blog.errors.any? %>
    <div class="flash flash--danger">
      <h2>Welp, the changes couldn't be saved.</h2>
      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, 'Blog title', class: 'form__label' %>
  <%= f.text_field :name, placeholder: 'This blog’s title…', class: 'form__input', required: true, autocomplete: 'off' %>

  <%= f.label :description, 'Blog description', class: 'form__label' %>
  <%= f.text_area :description, placeholder: 'This blog’s description…', required: true, class: 'form__input form__input--textarea' %>

  <%= f.label :about, 'About this blog', class: 'form__label' %>
  <%= f.text_area :about, placeholder: 'The content of this blog’s about page…', required: true, class: 'form__input form__input--textarea' %>

  <%= f.label :domain, 'Domain', class: 'form__label' %>
  <%= f.text_field :domain, placeholder: 'example.com', class: 'form__input', required: true, autocomplete: 'off' %>

  <%= f.label :short_domain, 'Short domain', class: 'form__label' %>
  <%= f.text_field :short_domain, placeholder: 'xmpl.co', class: 'form__input', required: true, autocomplete: 'off' %>

  <%= f.label :copyright, 'Copyright', class: 'form__label' %>
  <%= f.text_field :copyright, placeholder: 'Your copyright information…', class: 'form__input', required: true, autocomplete: 'off' %>

  <h2>Social</h2>

  <%= f.label :email, 'Email', class: 'form__label' %>
  <%= f.email_field :email, placeholder: 'Your contact email address…', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :facebook, 'Apple News', class: 'form__label' %>
  <%= f.text_field :apple_news_url, placeholder: 'Apple News URL', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :facebook, 'Facebook', class: 'form__label' %>
  <%= f.text_field :facebook, placeholder: 'Your Facebook profile or page, e.g. https://www.facebook.com/allencompassingtrip', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :flickr, 'Flickr', class: 'form__label' %>
  <%= f.text_field :flickr, placeholder: 'Your Flickr profile, e.g. https://www.flickr.com/photos/gesteves/', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :instagram, 'Instagram', class: 'form__label' %>
  <%= f.text_field :instagram, placeholder: 'Your Instagram profile, e.g. https://www.instagram.com/gesteves/', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :tumblr, 'Tumblr', class: 'form__label' %>
  <%= f.text_field :tumblr, placeholder: 'Your Tumblr blog, e.g. https://gesteves.tumblr.com/', class: 'form__input', autocomplete: 'off' %>

  <%= f.label :twitter, 'Twitter', class: 'form__label' %>
  <%= f.text_field :twitter, placeholder: 'Your Twitter profile, e.g. https://twitter.com/gesteves', class: 'form__input', autocomplete: 'off' %>

  <h2>Logos</h2>

  <%= f.label :header_logo_svg, 'Header logo', class: 'form__label' %>
  <%= f.text_area :header_logo_svg, placeholder: 'SVG for the main logo in the header…', class: 'form__input form__input--textarea' %>

  <h2>Icons</h2>

  <%= f.label :logo, 'Logo', class: 'form__label' %>
  <% if @blog.logo.present? %>
    <div><%= image_tag @blog.logo_url %>  </div>
  <% end %>
  <%= f.file_field :logo, accept: '.png', class: 'form__input form__input--tiny' %>

  <%= f.label :favicon, 'Favicon', class: 'form__label' %>
  <% if @blog.favicon.present? %>
    <div><%= image_tag @blog.favicon_url %>  </div>
  <% end %>
  <%= f.file_field :favicon, accept: '.png', class: 'form__input form__input--tiny' %>

  <%= f.label :touch_icon, 'Icon', class: 'form__label' %>
  <% if @blog.touch_icon.present? %>
    <div><%= image_tag @blog.touch_icon_url %>  </div>
  <% end %>
  <%= f.file_field :touch_icon, accept: '.png', class: 'form__input form__input--tiny' %>

  <h2>More options</h2>
  <%= f.label :show_related_entries, class: 'form__label form__label--checkbox' do %><%= f.check_box :show_related_entries %> Show related entries<% end %>

  <%= f.label :posts_per_page, class: 'form__label' %>
  <%= f.number_field :posts_per_page, min: 1, max: 120, pattern: '\d+', class: 'form__input', required: true %>

  <%= f.label :additional_meta_tags, 'Additional meta tags', class: 'form__label' %>
  <%= f.text_area :additional_meta_tags, placeholder: 'For example, Google Webmaster Tools verification tags…', class: 'form__input form__input--textarea' %>

  <div class="form__actions">
    <div class="form__buttons">
      <%= link_to 'Cancel', admin_entries_path, data: { confirm: 'Are you sure you want to discard this?' }, class: 'button button--danger' %>
    </div>
    <div class="form__buttons form__buttons--safe">
      <%= f.submit 'Save changes', class: 'button button--success' %>
    </div>
  </div>

<% end %>
