<% photo = entry.photos&.first %>
<div class="card-image">
  <a class="image image-container" href="<%= entry.permalink_url %>">
    <%= responsive_image_tag(
      photo: photo,
      srcset: @srcset,
      sizes: @sizes,
      html_options: {
        class: 'blurhash',
        loading: 'lazy',
        style: blurhash_background(photo),
        'data-controller': 'blurhash',
        'data-action': 'load->blurhash#removeBackground'
      }) %>
  </a>
</div>
