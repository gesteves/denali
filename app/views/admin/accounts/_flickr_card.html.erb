<div id="flickr-card">
  <h2 class="subtitle is-size-5-mobile">Flickr</h2>

  <% if social_account&.persisted? %>
    <div class="box">
      <div class="level is-mobile">
        <div class="level-left">
          <div class="level-item">
            <div>
              <p class="has-text-weight-semibold mb-2">
                <%= link_to social_account.handle, "https://flickr.com/people/#{social_account.handle}", target: "_blank", rel: "noopener" %>
              </p>
              <div class="tags has-addons">
                <span class="tag is-success">Added</span>
                <span class="tag"><%= time_ago_in_words(social_account.created_at) %> ago</span>
              </div>
            </div>
          </div>
        </div>
        <div class="level-right">
          <div class="level-item">
            <%= button_to 'Disconnect', flickr_admin_accounts_path,
                method: :delete,
                form: { data: { turbo_confirm: 'Disconnect this Flickr account?' } },
                class: 'button is-danger is-small' %>
          </div>
        </div>
      </div>
    </div>
  <% elsif ENV['FLICKR_CONSUMER_KEY'].present? && ENV['FLICKR_CONSUMER_SECRET'].present? %>
    <% if error.present? %>
      <div class="notification is-danger is-light">
        <%= error %>
      </div>
    <% end %>
    <%= button_to flickr_admin_accounts_path,
        method: :post,
        form: { data: { turbo: false } },
        class: 'button' do %>
      <span class="icon"><i class="fa-brands fa-flickr"></i></span>
      <span>Connect with Flickr</span>
    <% end %>
  <% else %>
    <p class="has-text-grey">Flickr API credentials are not configured.</p>
  <% end %>
</div>
