<div class="field is-horizontal">
  <div class="field-label is-normal">
    <%= f.label :url, 'URL', class: 'label' %>
  </div>
  <div class="field-body">
    <div class="field">
      <div class="control is-expanded">
        <%= f.text_field :url, value: @webhook.url, autocomplete: 'off', required: true, placeholder: 'The URL to notify…', class: 'input' %>
      </div>
      <p class="help">The webhook will be sent as a POST request.</p>
    </div>
  </div>
</div>

<hr class="hr">

<div class="buttons is-right">
  <%= f.submit 'Save changes', class: 'button is-success' %>
</div>
