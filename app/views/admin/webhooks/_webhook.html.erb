<tr>
<td>
  <%= webhook.url %>
</td>
<td>
  <div class="buttons is-pulled-right">
    <%= link_to 'Edit', edit_admin_webhook_path(webhook.id), class: 'button is-small' %>
    <%= link_to 'Delete', admin_webhook_path(webhook.id), method: :delete, data: { confirm: 'Are you absolutely sure you want to delete it forever? This can’t be undone!' }, class: 'button is-danger is-small' %>
  </div>
</td>
</tr>
