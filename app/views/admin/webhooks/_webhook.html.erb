<tr>
<td>
  <%= webhook.url %>
</td>
<td>
  <div class="buttons is-pulled-right">
    <%= link_to 'Edit', edit_admin_webhook_path(webhook.id), class: 'button is-small' %>
    <%= button_to 'Delete', admin_webhook_path(webhook.id), method: :delete, class: 'button is-danger is-small', form: { data: { turbo_confirm: "Are you absolutely sure you want to delete it forever? This can\u2019t be undone!" } } %>
  </div>
</td>
</tr>
