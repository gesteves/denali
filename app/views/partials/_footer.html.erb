<% cache "blog/footer/#{Time.now.strftime('%Y')}/#{@photoblog.id}/#{@photoblog.updated_at.to_i}" do %>
  <footer class="footer">
      <p>&copy; <%= @photoblog.entries.published.try(:last).try(:published_at).try(:strftime, '%Y') %>-<%= Time.now.strftime('%Y') %> <a href="http://www.gesteves.com" rel="me">Guillermo Esteves</a>. Have a nice day.</p>
  </footer>
<% end %>
