<div class="entry__figure" style="<%= "#{css_gradient_stops(photo)}#{css_dimensions(photo)}" %>">
  <%= responsive_image_tag(
    photo: photo,
    srcset: @srcset,
    sizes: @sizes,
    html_options: {
      class: 'entry__photo entry__photo--zoom entry__photo--lazy',
      loading: 'lazy',
      'data-action': 'click->photo#zoom keydown->photo#zoom contextmenu->photo#disableContextMenu',
      'data-photo-target': 'photo'
    }) %>
</div>
