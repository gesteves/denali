<h2 class="sr-only" id="tags">Tags</h2>
<ul class="tags" aria-labelledby="tags">
  <% tags.sort_by { |t| t.name }.each do |tag| %>
    <li><%= link_to tag.name, tag_url(tag.slug), { rel: 'tag', title: "All photos tagged “#{tag.name}”" } %></li>
  <% end %>
</ul>
