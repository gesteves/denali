<% cache [@photoblog, page_url] do %>
  <%= render partial: 'entries/grid/entry', object: entries.shift, as: :entry, locals: { page_url: page_url } %>
  <%= render partial: 'entries/grid/entry', collection: entries, as: :entry %>
<% end %>
