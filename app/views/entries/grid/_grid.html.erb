<% cache [@photoblog, page_url] do %>
  <%= render partial: 'entries/grid/observer', object: entries.shift, as: :entry, locals: { page_url: page_url, total_entries: total_entries, page: page, per_page: per_page } %>
  <%= render partial: 'entries/grid/regular', collection: entries, as: :entry, locals: { total_entries: total_entries, page: page, per_page: per_page, offset: 1 }, cached: true %>
<% end %>
