<li class="entry-list__item">
  <% photo = entry.association(:photos).loaded? ? entry.photos[0] : entry.photos.first %>
  <%= render partial: 'entries/list/entry_link', object: entry, as: :entry, locals: { photo: photo } %>
</li>
