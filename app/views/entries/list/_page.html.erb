<% cache [entries, page_url] do %>
  <%= render partial: 'entries/list/entry_observer', object: entries.shift, as: :entry, locals: { page_url: page_url }  %>
  <%= render partial: 'entries/list/entry', collection: entries, as: :entry, cached: true  %>
<% end %>
