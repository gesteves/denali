<% photo = entry.photos.first %>
<a class="entry-list__link" href="<%= entry.permalink_url %>" aria-labelledby="<%= "entry-list-caption-#{entry.id}" target="_blank" %>">
  <%= responsive_image_tag(
    photo: photo,
    srcset: @srcset,
    sizes: @sizes,
    aspect_ratio: local_assigns[:aspect_ratio].presence,
    html_options: {
      alt: '',
      class: 'entry-list__image placeholder',
      loading: 'lazy',
      style: css_placeholder_background(photo),
      'aria-hidden': true,
      'data-controller': 'placeholder',
      'data-action': 'load->placeholder#removeBackground'
    }) %>
  <div class="entry-list__photo-caption" id="<%= "entry-list-caption-#{entry.id}" %>">
    <h3 class="entry-list__photo-title"><%= truncate(entry.plain_title, length: 80, separator: ' ', omission: '…') %></h3>
    <p class="entry-list__photo-meta">Published on <%= entry.published_at.strftime('%B %-d, %Y') %></p>
  </div>
</a>
