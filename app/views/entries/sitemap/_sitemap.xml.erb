<sitemap>
  <loc><%= sitemap_url(page: page) %></loc>
  <lastmod><%= @photoblog.entries.published('published_at ASC').page(page).per(@entries_per_sitemap).pluck(:modified_at).max.strftime('%Y-%m-%dT%H:%M:%S%:z') %></lastmod>
</sitemap>
