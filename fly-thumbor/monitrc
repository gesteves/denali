# Monit configuration - disk monitoring only
# Process management handled by supervisord

set daemon 60
set log /dev/stdout

# Disk space monitoring - trigger cleanup when storage exceeds 80%
check filesystem storage with path /
    if space usage > 80% then exec "/usr/local/bin/cleanup.sh"
